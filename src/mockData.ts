export const mockSteps = [
  {
    cardLogicTitle: "First card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "First card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 1,
  },
  {
    cardLogicTitle: "Second card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Second card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 2,
  },
  {
    cardLogicTitle: "Third card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Third card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 3,
  },
  {
    cardLogicTitle: "Fourth card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Fourth card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 4,
  },
  {
    cardLogicTitle: "Fifth card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Fifth card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 5,
  },
  {
    cardLogicTitle: "Sixth card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Sixth card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 6,
  },
  {
    cardLogicTitle: "Seventh card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Seventh cardSome description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 7,
  },
  {
    cardLogicTitle: "Eighth card",
    cardTechnicalTitle: "[Technical details]",
    cardLogicText:
      "Eighth card Some description for card with logic for showing in UI",
    cardTechnicalText: `If you're using Gitpod, you can skip this step.
Make sure Rust is installed per the prerequisites in near-sdk-rs
Ensure near-cli is installed by running near --version. If not installed, install with: 
npm install -g near-cli`,
    position: 8,
  },
];

export const mockHomeTitle = {
  firstCard:
    "Sending fungible tokens to an account that doesn't have their name in the mapp",
  secondCard: "Sending FTs to an account and adding them",
  thirdCard:
    "Sending 5 FTs to a contract that wants a payment of 3 FTs for some service and seeing that 2 FTs are returned in change.",
  fourthCard:
    "Sending 3 FTs to a contract that wants a payment of 3 FTs for some service",
};
